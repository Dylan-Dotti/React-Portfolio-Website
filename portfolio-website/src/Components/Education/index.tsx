import React, { ReactNode } from "react";

export default class Education extends React.Component {

    render(): ReactNode {
        return (
            <p>Education component works!</p>
        );
    }
}